import {run} from '~/run'

function solve(input: string) {
  return 'solution'
}

run({
  solve,
  tests: [
    {
      input: 'test.txt',
      expected: '',
    },
    {
      input: 'input.txt',
      expected: '',
    },
  ],
})
    