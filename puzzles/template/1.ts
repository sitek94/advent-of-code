import {run} from '~/run'

function solve(input: string) {
  return 0
}

run({
  solve,
  tests: [
    {
      input: 'test.txt',
      expected: 100,
    },
    {
      input: 'input.txt',
      expected: 100,
    },
  ],
})
    